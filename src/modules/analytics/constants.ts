export const ANALYTICS_METRICS_VERSION = "analytics-v1"

// Bump this version only when analytics outputs can change for the same source
// data, for example after a KPI formula, bucket, active-state, or currency
// semantics change.
